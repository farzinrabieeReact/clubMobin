import React from "react";

const ButtonOutLine = ({ title }) => {
  return (
    <>
      <button className="btn backGroundOrangeOutLine">{title}</button>
    </>
  );
};

export default ButtonOutLine;
